<?php namespace {{ namespace }};

use Mosaiqo\OptimusPrime\Transformer;
use Mosaiqo\OptimusPrime\Contracts\Transformer;

class {{ name }} extends Transformer implements Transformer{

    /**
     * @param array $entity
     */
    public function transform(array $entity)
    {
			return [
        {{# properties}}
        {{ . }} = $entity['{{ . }}'];
        {{/ properties}}
        ];
    }

}